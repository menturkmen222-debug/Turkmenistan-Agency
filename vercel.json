{
  "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @workspace/yenil run build",
  "outputDirectory": "artifacts/yenil/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
